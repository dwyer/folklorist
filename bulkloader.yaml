# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: BalladIndex
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: index
      external_name: index

    - property: name
      external_name: name


- kind: BalladReference
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: name
      external_name: name

    - property: title
      external_name: title


- kind: BalladFile
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: alternate_titles
      external_name: alternate_titles

    - property: author
      external_name: author

    - property: broadsides
      external_name: broadsides
      import_transform: db.Text

    - property: cross_references
      external_name: cross_references

    - property: description
      external_name: description

    - property: earliest_date
      external_name: earliest_date

    - property: file
      external_name: file

    - property: found_in
      external_name: found_in

    - property: historical_references
      external_name: historical_references
      import_transform: db.Text

    - property: keywords
      external_name: keywords

    - property: long_description
      external_name: long_description
      import_transform: db.Text

    - property: name
      external_name: name

    - property: notes
      external_name: notes
      import_transform: db.Text

    - property: recordings
      external_name: recordings
      import_transform: db.Text

    - property: references
      external_name: references
      import_transform: db.Text

    - property: same_tune
      external_name: same_tune
      import_transform: db.Text


- kind: SuppTradFile
  connector: csv
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: file
      external_name: file

    - property: name
      external_name: name

    - property: text
      external_name: text
      import_transform: db.Text

